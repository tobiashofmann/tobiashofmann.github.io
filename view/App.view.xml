<mvc:View
	controllerName="abapconf.web.abapconf.controller.App"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">
    <Shell xmlns="sap.m" appWidthLimited="false">
        <App
            id="app"
            busy="{appView>/busy}"
            busyIndicatorDelay="{appView>/delay}"/>
    </Shell>
</mvc:View>
